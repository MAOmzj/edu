# 文件用途：配置应用名称、学科、Agent重试、教育Skill、上下文摘要、联网和长期记忆。
# 调用关系：utils/config_handler.py 加载本文件；Agent、Context Manager、Web和存储模块读取对应字段。
# 修改易踩坑：新增字段要同步 config_handler 校验；摘要保留数必须小于触发数，学科名要全项目一致。
name: 小学知识小助手
supported_subjects:
  - 语文
  - 数学
  - 英语
  - 科学
  - 安全与品德
  - 综合

agent:
  recursion_limit: 12
  # 初次审核不通过后最多重写一次，防止“回答—审核”无限循环。
  max_review_retries: 1

# 教育 Skill 规定“怎样教学”，Tool 负责真正查询知识或执行计算。
# 学科 Agent 只加载当前学科对应的 Skill，避免数学规则影响其他学科。
skills:
  enabled: true
  max_instruction_chars: 6000
  subject_paths:
    数学: skills/primary-math/SKILL.md

# 统一 Context Manager 的上下文预算。最近消息优先，较旧或低优先级内容先裁剪。
context:
  # Prompt 中最多选择的最近原始消息数量。
  recent_message_limit: 12
  max_conversation_chars: 8000
  max_long_term_chars: 2000
  max_review_feedback_chars: 1200
  # 单机生产基线：约 12 轮问答或会话文字超过 8000 字符时触发增量摘要。
  # 工作流在当前学生问题已经进入、助手尚未回答时执行压缩，因此保留 11 条
  # 可覆盖“最近 5 轮完整问答 + 当前问题”，避免从问答对中间截断。
  summary_enabled: true
  summary_trigger_messages: 24
  summary_keep_recent_messages: 11
  summary_trigger_chars: 8000
  max_summary_chars: 2000

# Tavily 只补充本地知识库缺少的资料和时效信息，不替代本地 RAG。
web_search:
  enabled: true
  # 审核 Agent 是否也能联网。默认关闭可减少延迟和 Tavily 调用次数。
  review_enabled: false
  max_results: 3
  max_query_chars: 200
  summary_max_chars: 600
  search_depth: basic
  # 开启后，发送给 Tavily 的 include_domains 和返回结果都会受到白名单限制。
  trusted_domains_only: true
  trusted_domains:
    - gov.cn
    - moe.gov.cn
    - pep.com.cn
    - cas.cn
    - cast.org.cn
    - cdstm.cn
    - nasa.gov
    - noaa.gov
    - usgs.gov
    - si.edu
    - who.int
    - unicef.org
    - unesco.org

memory:
  database_path: storage/education_memory.sqlite3
  # SQLite 仅作为单机生产试运行方案；多副本部署前应迁移到共享数据库。
  max_long_term_memories: 200
  retrieval_limit: 5
