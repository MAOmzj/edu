# 文件用途：声明运行后端、Agent、RAG、SQLite、Chroma、Tavily和Web服务所需的Python依赖范围。
# 调用关系：pip install -r requirements.txt 读取本文件；Python代码随后导入这些第三方包。
# 修改易踩坑：大版本升级可能破坏LangChain/LangGraph接口，Chroma在Windows上的原生兼容性要单独验证。
python-dotenv>=1.2,<2
PyYAML>=6.0,<7
langchain>=1.3,<2
langchain-core>=1.5,<2
langchain-community>=0.4,<0.5
langchain-chroma>=1.1,<2
langchain-deepseek>=1.1,<2
langchain-tavily>=0.2,<1
langchain-text-splitters>=1.1,<2
langgraph>=1.2,<2
langgraph-checkpoint-sqlite>=3.0,<4
chromadb==1.3.5
dashscope>=1.26,<2
pypdf>=6.0,<7
fastapi>=0.116,<1
uvicorn[standard]>=0.35,<1
